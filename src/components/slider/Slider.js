import { React, useState } from 'react'
import "./Slider.scss";

const Slider = () => {
    const [currentSlide, setCurrentSlide] = useState(0);

  return (
    <div>
        <h1>slider</h1>
    </div>
  )
}

export default Slider